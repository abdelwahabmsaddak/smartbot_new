services:
  - type: web
    name: smartbot_new
    env: python
    region: frankfurt
    branch: main
    buildCommand: |
      pip install -r requirements.txt
      chmod +x start.sh
    startCommand: ./start.sh
    autoDeploy: true
